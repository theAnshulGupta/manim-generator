Module provides utilities for configuring Manim's logging system, including creating a logger with rich text formatting, parsing color themes, and setting up file logging. Key functions include `make_logger()` for creating the logger and consoles, `parse_theme()` for configuring logger styles, and `set_file_logger()` for adding file logging. The module also defines a `JSONFormatter` for logging records in JSON format, primarily used for internal testing.