Module provides utility function `get_full_sound_file_path()` which takes a sound file name as input and returns its full file path, helpful for locating sound files within the Manim library's directory structure.