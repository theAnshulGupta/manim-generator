The Triangle class in Manim is a subclass of RegularPolygon that creates an equilateral triangle. It accepts additional keyword arguments and can be easily manipulated through methods like scale() and rotate(). The example demonstrates creating two triangles of different sizes and rotations, arranged together in a group, showcasing the basic usage of this geometric mobject.