The `SceneFileWriter` class is responsible for writing Manim animations to video files, managing partial movie files, audio, and sections. Key functionalities include initializing output directories, handling audio segments, writing frames, combining partial movie files into a final video, managing cache, and supporting section-based video creation. It supports various output formats like MP4, WebM, and GIF, and can handle sound integration, frame writing, and metadata management for rendered animations.