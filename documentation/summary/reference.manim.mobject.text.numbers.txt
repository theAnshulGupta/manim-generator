A module containing mobjects for representing numerical values. It includes three primary classes: DecimalNumber (for displaying decimal numbers), Integer (for displaying whole numbers), and Variable (for displaying text with a label and continuously updated value from a ValueTracker). These classes help create dynamic and customizable numeric representations in Manim animations.