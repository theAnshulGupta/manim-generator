Provides utility functions for handling configuration dictionaries in Manim: `merge_dicts_recursively()` merges multiple dictionaries recursively with later dictionaries having higher priority, `update_dict_recursively()` updates a dictionary using merged results, and `DictAsObject` allows dictionary-like object attribute access. Additionally includes descriptor classes `_Data` and `_Uniforms` for managing grouped data and uniforms attributes.