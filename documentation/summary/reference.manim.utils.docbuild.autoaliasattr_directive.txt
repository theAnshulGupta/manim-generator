This module provides a custom Sphinx directive for documenting type aliases and module-level attributes in Manim's documentation. The key component is `AliasAttrDocumenter`, which creates a "Type Aliases" section that specifically identifies and documents module-level attributes annotated as TypeAlias. The module also includes a `smart_replace()` utility function for handling complex alias substitutions in documentation strings.